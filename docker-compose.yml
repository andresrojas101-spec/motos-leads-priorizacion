services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: motos
      POSTGRES_PASSWORD: motos
      POSTGRES_DB: motos_leads
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
