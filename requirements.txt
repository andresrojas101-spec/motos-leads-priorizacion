# Fase 1 - ingesta, normalizacion, deduplicacion
pandas>=2.2
SQLAlchemy>=2.0
rapidfuzz>=3.9
python-dotenv>=1.0
psycopg[binary]>=3.1  # driver Postgres -- SQLite en dev local, Postgres en prod (DATABASE_URL)

# Fase 2 - extraccion con IA (validacion de salida estructurada)
pydantic>=2.7
openai>=1.50      # proveedor por defecto: habla con Ollama local via su API compatible
groq>=0.11        # alternativo (capa gratuita en la nube), ver PROVEEDOR_LLM en .env
anthropic>=0.40   # alternativo de pago, ver PROVEEDOR_LLM en .env

# Desarrollo
pytest>=8.0
